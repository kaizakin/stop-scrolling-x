# Stop Scrolling

A small Chrome extension that blocks `x.com` for a fixed timer.

Build with `npm run build`, then load the `dist/` folder as an unpacked extension in Chrome.
or download from the release [here](https://github.com/kaizakin/stop-scrolling-x/releases/tag/v1.0.0)
